// Mantido por compatibilidade: a lógica de caixa livre vive agora em lib/caixa.ts
// (fonte única de verdade, usada tambem por cashFlowService e pelo dashboard).
export { calcularCaixaLivre } from './caixa';
